/* UNITCPP: Switch private members references to getter methods,
            - safety measures for the base - cannot move, attack, get built, build more than one unit at once
            - safety measures for attacking own units
            After map implementation:
            - isValidPosition, to check for obstacles and if they can be jumped over, enemy's units and base, map's boundaries
            */
/* UNITHPP: Add method descriptions, briefs, return values, params. */

/* TODO: Add map implementation, makefile, player ai, mediator, main, docs, readme, catch, unit tests */